"use strict";var _seedCourses = await jitiImport("../server/utils/seedCourses.ts");
var _prisma = _interopRequireDefault(await jitiImport("../server/utils/prisma.ts"));function _interopRequireDefault(e) {return e && e.__esModule ? e : { default: e };}

async function runSeed() {
  console.log('Seeding courses into PostgreSQL database...');
  await (0, _seedCourses.seedCoursesIfEmpty)();
  const courses = await _prisma.default.course.findMany();
  console.log(`Successfully seeded ${courses.length} courses:`, courses.map((c) => c.title));
  await _prisma.default.$disconnect();
}

runSeed(); /* v9-bfa4f78d72cf16ac */
